{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "PomoTime (Hot Reload)",
      "type": "coreclr",
      "request": "launch",
      "program": "dotnet",
      "args": ["watch", "run", "--project", "${workspaceFolder}/PomoTime.csproj"],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}

